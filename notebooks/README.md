# Notebooks

These notebooks are stripped-down follow-along companions to the textbook chapters.
Before opening them, create a symlink so the notebooks can find the data files:

```bash
ln -s ../data notebooks/data
```

Alternatively, you can simply copy the `data/` folder into `notebooks/`.
